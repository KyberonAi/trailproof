# Plan: <feature-title>

## Spec
.claude/specs/<feature-slug>.md

## Branch
feature/<feature-slug>

## Tasks

A numbered checkbox task list. Each task is one commit's worth of work.

- [ ] **Task 1:** Description — scope: `python` | `typescript` | `both`, complexity: `small` | `medium` | `large`
- [ ] **Task 2:** Description — scope: `python` | `typescript` | `both`, complexity: `small` | `medium` | `large`

### Task Order
1. Python tasks first
2. TypeScript mirror tasks after
3. Shared test vector tasks last

### Dependencies
Call out dependencies between tasks (e.g., "Task 3 depends on Task 1").
